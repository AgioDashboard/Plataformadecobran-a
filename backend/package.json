{
  "name": "plataforma-cobranca-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/gerar-painel.mjs && wrangler dev --test-scheduled",
    "deploy": "node scripts/gerar-painel.mjs && wrangler deploy",
    "teste": "node --test \"testes/*.test.ts\"",
    "migrar": "wrangler d1 migrations apply cobranca --local",
    "migrar:remoto": "wrangler d1 migrations apply cobranca --remote"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "@types/node": "^26.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
